<ul class="govuk-list">
  <% reasons.each do |reason| %>
    <li>
      <h5 class="govuk-heading-xs govuk-!-margin-0">
        <%= t("significant_dates.type.#{reason.reason_type}") if reason.reason_type.present? %>
      </h5>
      <%= reason.note.body %>
    </li>
<% end %>
</ul>
