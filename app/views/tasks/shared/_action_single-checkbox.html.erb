<%= form.govuk_check_box :completed_action_ids,
                         action.id,
                         label: { text: action.title },
                         link_errors: true,
                         checked: action.completed?,
                         hint: -> do %>
  <%= yield %>
<% end %>
