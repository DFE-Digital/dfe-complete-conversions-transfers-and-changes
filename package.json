{
  "license": "MIT",
  "scripts": {
    "lint:format": "prettier --check '**/*'",
    "lint:format:fix": "prettier --write '**/*'",
    "lint:js": "eslint .",
    "lint:js:fix": "yarn run lint:js --fix",
    "workflows:validate": "ajv -s app/workflows/schemas/task-list.json -d app/workflows/*.yml --spec=draft2020"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "eslint": "^8.22.0",
    "eslint-config-prettier": "^8.5.0",
    "govuk-frontend": "^4.3.1",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "@ministryofjustice/frontend": "^1.4.2"
  }
}
