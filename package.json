{
  "license": "MIT",
  "scripts": {
    "lint:format": "prettier --check '**/*'",
    "lint:format:fix": "prettier --write '**/*'",
    "lint:js": "eslint .",
    "lint:js:fix": "yarn run lint:js --fix",
    "workflows:validate": "ajv -s workflows/schemas/task-list.json -d workflows/*.yml --spec=draft2020"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "eslint": "^8.18.0",
    "eslint-config-prettier": "^8.5.0",
    "govuk-frontend": "^4.2.0",
    "prettier": "^2.7.1",
    "eslint": "^8.19.0",
    "eslint-config-prettier": "^8.5.0"
  }
}
